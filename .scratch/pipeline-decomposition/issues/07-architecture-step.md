# 07 — Design the architecture step

Labels: `wayfinder:grilling`

Blocked by: 01

## Question

Adapt `improve-codebase-architecture` into the pre-PR architecture step:

- It never fixes in the run; it writes findings as `ready-for-human` tracker issues to grill/refine/drop later.
- What its finding format is, and how it reuses vs diverges from the existing HTML-report skill (which is interactive/HITL by design).

Produces the architecture-step spec.
